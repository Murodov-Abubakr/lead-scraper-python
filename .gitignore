# Secret keys - NEVER push these
.env

# Data files with personal info
outreach_sent.json
outreach_sent_backup*.json
email_sequence.json
dental_leads.csv
qualified_leads.csv
agencies.csv
results*.csv
stores.csv
hiring_signals.csv

# Chatbot runtime data — patient PII and analytics (never commit)
chatbot/appointments.csv
chatbot/analytics.json
chatbot/analytics.json.tmp
chatbot/credentials.json

# Log files (but not requirements files)
*.log
outreach_log.txt
!requirements*.txt

# Screenshots folder (large files)
screenshots/

# Media files
*.mp3
*.mp4
videos/

# Python cache
__pycache__/
*.pyc
*.pyo

# WebDriver cache
.wdm/

# Dashboard dependencies
dashboard/node_modules/
replied.json
owner_outreach.json

# Mobile APK build output (not the android project itself — that IS committed)
dashboard/android/app/build/
dashboard/android/.gradle/

# Log output files (runtime stdout/stderr captures)
*_log.txt
*_err.txt
*_out.txt
overnight_log.txt
push250_log.txt

# Third-party OSINT tools (cloned separately, not part of this repo)
recon_ng_tool/
